export enum StatsMethods {
    COUNT = 'COUNT',
    SUM = 'SUM',
    AVG = 'AVG',
}

export enum StepType {
    MINUTES = 'MINUTES',
    HOURS = 'HOURS',
    DAYS = 'DAYS',
    WEEKS = 'WEEKS',
    MONHTHS = 'MONTHS',
    YEARS = 'YEARS',
}
